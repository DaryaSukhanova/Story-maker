<svg xmlns="http://www.w3.org/2000/svg" width="1000" height="1000" viewbox="0 0 1000 1000"><rect stroke="black" stroke-width="2" fill="none" x="182" y="44" width="99" height="100" id="animatedElementId"></rect><path stroke="red" stroke-width="2" fill="none" id="motionPath" d="M 258 165 L 229 165 L 226 165 L 242 163 L 292 157 L 366 153 L 456 150 L 565 148 L 641 148 L 679 154 L 683 159 L 668 169 L 625 190 L 544 224 L 473 258 L 428 280 L 419 287 L 429 287 L 468 283 L 535 279 L 591 276 L 621 276 L 627 277 L 623 281 L 614 292 L 605 312 L 603 329 L 605 339 L 608 345 L 608 346"></path><script>
            <![CDATA[
                
                function animate(element) {
    const motionPath = document.getElementById('motionPath');
    const totalLength = motionPath.getTotalLength();
    let distanceCovered = 0;
    const speed = 5;
    const moveAlongPath = () => {
      this.play = true;
      if (this.play) {
        const point = motionPath.getPointAtLength(distanceCovered);
        element.setAttribute("transform", `translate(${point.x} ${point.y})`);
        distanceCovered += speed;
        if (distanceCovered <= totalLength) {
          requestAnimationFrame(moveAlongPath);
        } else {
          // console.log(this.animations)
          distanceCovered = 0;
          requestAnimationFrame(moveAlongPath);
          this.saveAnimatedSvg();
        }
      } else {
        requestAnimationFrame(moveAlongPath);
      }
    };
    moveAlongPath();
  }
                const animatedElement = document.getElementById('animatedElementId');
                animate(animatedElement);
            ]]>
        </script></svg>